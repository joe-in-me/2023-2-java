* 다중상속 *
클래스, 추상클래스 모두 다중상속이 안됨
ㄴ> 구현부가 있을 확률이 높으므로, 메소드 이름이 동일할 시,
    상속클래스에서 super 메소드를 호출하면, 어떤 메소드를 호출해야할지
    알 수 없기 때문이다.

인터페이스 는 다중상속이 됨
ㄴ> 이경우, default 메소드가 있다면 어떻게 될지 궁금했는데,
    실험을 해보니, interface 를 다중상속하는 경우, super 클래스들의 메소드들중,
    이름이 같은게 있으면, '구현' 을 하라고 컴파일 에러를 낸다.


* 익명 클래스 *
인터페이스, 추상클래스 뿐만 아니라, 일반 클래스도 익명클래스로 Override 가 가능하다.
사용하는 이유는 일부 메소드의 기능을 재정의 하기 위해 주로 사용하겠다.
다만 잘 사용하지는 않는다고 한다.

